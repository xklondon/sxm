/** Client-only delay before Round Summary overlay after final cards are visible. */
export const ROUND_SUMMARY_OVERLAY_DELAY_MS = 3000;

/** Mobile game-over overlay — hold final bank cards readable before summary sheet. */
export const MOBILE_GAME_OVER_OVERLAY_DELAY_MS = 3000;
